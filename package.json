{
  "name": "makebuildform",
  "version": "1.0.0",
  "description": "A smooth, vertical scrolling multi-step form experience",
  "main": "dist/mbf.js",
  "scripts": {
    "dev": "http-server -p 5500 -c-1 --cors",
    "build": "mkdir -p dist/css && cp -r src/js/* dist/ && cp -r src/css dist/ ",
    "watch": "nodemon --watch src -e js,css,html --exec 'npm run build'",
    "start": "npm run build && npm run dev && npm run watch"

  },
  "keywords": [
    "form",
    "multi-step",
    "validation",
    "navigation"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1",
    "nodemon": "^2.0.22",
    "vite": "^6.2.5"
  }
}
